# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# Note: This name is referred to in the test job, so make sure any changes are sync'd up!
# Further this is referencing a run in the backport branch to fetch old bindings.
name: "CI"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}
  cancel-in-progress: true

on:
  push:
    branches:
      - "pull-request/[0-9]+"
      - "main"
  workflow_dispatch:

jobs:
  ci-vars:
    runs-on: ubuntu-latest
    outputs:
      CUDA_BUILD_VER: ${{ steps.get-vars.outputs.cuda_build_ver }}
      CUDA_PREV_BUILD_VER: ${{ steps.get-vars.outputs.cuda_prev_build_ver }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          fetch-depth: 0

      - name: Get CUDA build versions
        id: get-vars
        run: |
          cuda_build_ver=$(yq '.cuda.build.version' ci/versions.yml)
          echo "cuda_build_ver=$cuda_build_ver" >> $GITHUB_OUTPUT
          cuda_prev_build_ver=$(yq '.cuda.prev_build.version' ci/versions.yml)
          echo "cuda_prev_build_ver=$cuda_prev_build_ver" >> $GITHUB_OUTPUT

  should-skip:
    runs-on: ubuntu-latest
    outputs:
      skip: ${{ steps.get-should-skip.outputs.skip }}
      doc-only: ${{ steps.get-should-skip.outputs.doc_only }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
      - name: Compute whether to skip builds and tests
        id: get-should-skip
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euxo pipefail
          if ${{ startsWith(github.ref_name, 'pull-request/') }}; then
            pr_number="$(grep -Po '(\d+)$' <<< '${{ github.ref_name }}')"
            pr_title="$(gh pr view "${pr_number}" --json title --jq '.title')"
            skip="$(echo "${pr_title}" | grep -q '\[no-ci\]' && echo true || echo false)"
            doc_only="$(echo "${pr_title}" | grep -q '\[doc-only\]' && echo true || echo false)"
          else
            skip=false
            doc_only=false
          fi
          echo "skip=${skip}" >> "$GITHUB_OUTPUT"
          echo "doc_only=${doc_only}" >> "$GITHUB_OUTPUT"

  # LLVM build stages (matching GitLab build-llvm stage)
  build-llvm-linux-64:
    needs:
      - should-skip
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.skip) }}
    uses: ./.github/workflows/build-llvm.yml
    with:
      host-platform: linux-64
    secrets: inherit

  build-llvm-linux-aarch64:
    needs:
      - should-skip
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.skip) && !fromJSON(needs.should-skip.outputs.doc-only) }}
    uses: ./.github/workflows/build-llvm.yml
    with:
      host-platform: linux-aarch64
    secrets: inherit

  build-llvm-windows:
    needs:
      - should-skip
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.skip) && !fromJSON(needs.should-skip.outputs.doc-only) }}
    uses: ./.github/workflows/build-llvm.yml
    with:
      host-platform: win-64
    secrets: inherit

  # WARNING: make sure all of the build jobs are in sync
  build-linux-64:
    needs:
      - ci-vars
      - should-skip
      - build-llvm-linux-64
    strategy:
      fail-fast: false
      matrix:
        host-platform:
          - linux-64
    name: Build ${{ matrix.host-platform }}
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.skip) }}
    uses: ./.github/workflows/build-wheel.yml
    with:
      host-platform: ${{ matrix.host-platform }}
      cuda-version: ${{ needs.ci-vars.outputs.CUDA_BUILD_VER }}
      prev-cuda-version: ${{ needs.ci-vars.outputs.CUDA_PREV_BUILD_VER }}

  # WARNING: make sure all of the build jobs are in sync
  build-linux-aarch64:
    needs:
      - ci-vars
      - should-skip
      - build-llvm-linux-aarch64
    strategy:
      fail-fast: false
      matrix:
        host-platform:
          - linux-aarch64
    name: Build ${{ matrix.host-platform }}
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.skip) && !fromJSON(needs.should-skip.outputs.doc-only) }}
    uses: ./.github/workflows/build-wheel.yml
    with:
      host-platform: ${{ matrix.host-platform }}
      cuda-version: ${{ needs.ci-vars.outputs.CUDA_BUILD_VER }}
      prev-cuda-version: ${{ needs.ci-vars.outputs.CUDA_PREV_BUILD_VER }}

  # WARNING: make sure all of the build jobs are in sync
  build-windows:
    needs:
      - ci-vars
      - should-skip
      - build-llvm-windows
    strategy:
      fail-fast: false
      matrix:
        host-platform:
          - win-64
    name: Build ${{ matrix.host-platform }}
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.skip) && !fromJSON(needs.should-skip.outputs.doc-only) }}
    uses: ./.github/workflows/build-wheel.yml
    with:
      host-platform: ${{ matrix.host-platform }}
      cuda-version: ${{ needs.ci-vars.outputs.CUDA_BUILD_VER }}
      prev-cuda-version: ${{ needs.ci-vars.outputs.CUDA_PREV_BUILD_VER }}

  # WARNING: make sure both Linux test jobs are in sync
  test-linux-64:
    strategy:
      fail-fast: false
      matrix:
        host-platform:
          - linux-64
    name: Test ${{ matrix.host-platform }}
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.doc-only) }}
    permissions:
      contents: read  # This is required for actions/checkout
    needs:
      - should-skip
      - build-linux-64
    uses: ./.github/workflows/test-wheel-linux.yml
    with:
      build-type: pull-request
      host-platform: ${{ matrix.host-platform }}

  # WARNING: make sure both Linux test jobs are in sync
  test-linux-aarch64:
    strategy:
      fail-fast: false
      matrix:
        host-platform:
          - linux-aarch64
    name: Test ${{ matrix.host-platform }}
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.doc-only) }}
    permissions:
      contents: read  # This is required for actions/checkout
    needs:
      - should-skip
      - build-linux-aarch64
    uses: ./.github/workflows/test-wheel-linux.yml
    with:
      build-type: pull-request
      host-platform: ${{ matrix.host-platform }}

  test-windows:
    strategy:
      fail-fast: false
      matrix:
        host-platform:
          - win-64
    name: Test ${{ matrix.host-platform }}
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.doc-only) }}
    permissions:
      contents: read  # This is required for actions/checkout
    needs:
      - should-skip
      - build-windows
    uses: ./.github/workflows/test-wheel-windows.yml
    with:
      build-type: pull-request
      host-platform: ${{ matrix.host-platform }}

  test-expensive-linux-64:
    name: Expensive Test linux-64
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.doc-only) }}
    permissions:
      contents: read  # This is required for actions/checkout
    needs:
      - should-skip
      - build-linux-64
    uses: ./.github/workflows/test-expensive-linux.yml
    with:
      host-platform: linux-64

  test-expensive-linux-aarch64:
    name: Expensive Test linux-aarch64
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.doc-only) }}
    permissions:
      contents: read  # This is required for actions/checkout
    needs:
      - should-skip
      - build-linux-aarch64
    uses: ./.github/workflows/test-expensive-linux.yml
    with:
      host-platform: linux-aarch64

  test-cudf-source-linux-64:
    name: cudf source Test linux-64
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.doc-only) }}
    permissions:
      contents: read
    needs:
      - should-skip
      - build-linux-64
    uses: ./.github/workflows/test-cudf-source-linux.yml
    with:
      host-platform: linux-64

  test-cudf-source-linux-aarch64:
    name: cudf source Test linux-aarch64
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.doc-only) }}
    permissions:
      contents: read
    needs:
      - should-skip
      - build-linux-aarch64
    uses: ./.github/workflows/test-cudf-source-linux.yml
    with:
      host-platform: linux-aarch64

  test-numbast-mlir-linux-64:
    name: Numbast MLIR Test linux-64
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.doc-only) }}
    permissions:
      contents: read
    needs:
      - should-skip
      - build-linux-64
    uses: ./.github/workflows/test-numbast-mlir-linux.yml
    with:
      host-platform: linux-64

  test-numbast-mlir-linux-aarch64:
    name: Numbast MLIR Test linux-aarch64
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.doc-only) }}
    permissions:
      contents: read
    needs:
      - should-skip
      - build-linux-aarch64
    uses: ./.github/workflows/test-numbast-mlir-linux.yml
    with:
      host-platform: linux-aarch64

  test-nvmath-python-mlir-linux-64:
    name: nvmath-python MLIR Test linux-64
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.doc-only) }}
    permissions:
      contents: read
    needs:
      - should-skip
      - build-linux-64
    uses: ./.github/workflows/test-nvmath-python-mlir-linux.yml
    with:
      host-platform: linux-64

  test-nvmath-python-mlir-linux-aarch64:
    name: nvmath-python MLIR Test linux-aarch64
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.doc-only) }}
    permissions:
      contents: read
    needs:
      - should-skip
      - build-linux-aarch64
    uses: ./.github/workflows/test-nvmath-python-mlir-linux.yml
    with:
      host-platform: linux-aarch64

  test-cuda-gdb-linux-64:
    name: cuda-gdb Test linux-64
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.doc-only) }}
    permissions:
      contents: read
    needs:
      - should-skip
      - build-linux-64
    uses: ./.github/workflows/test-wheel-linux.yml
    with:
      build-type: pull-request
      host-platform: linux-64
      test-script: run-cuda-gdb-tests
      extra-cuda-components: "cuda_gdb,cuda_nvdisasm"
      matrix_filter: 'map(select(.PY_VER == "3.12" and .CUDA_VER == "13.4.1" and .LOCAL_CTK == "1" and .GPU == "l4")) | .[0:1]'

  test-cuda-gdb-linux-aarch64:
    name: cuda-gdb Test linux-aarch64
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.doc-only) }}
    permissions:
      contents: read
    needs:
      - should-skip
      - build-linux-aarch64
    uses: ./.github/workflows/test-wheel-linux.yml
    with:
      build-type: pull-request
      host-platform: linux-aarch64
      test-script: run-cuda-gdb-tests
      extra-cuda-components: "cuda_gdb,cuda_nvdisasm"
      matrix_filter: 'map(select(.PY_VER == "3.12" and .CUDA_VER == "13.4.1" and .LOCAL_CTK == "1" and .GPU == "a100")) | .[0:1]'

  # ============== old CI jobs (ci.yaml & pr.yaml) ==============

  build-docs:
    needs:
      - should-skip
      - build-linux-64
    if: ${{ github.repository_owner == 'nvidia' && !fromJSON(needs.should-skip.outputs.skip) }}
    permissions:
      actions: read
      contents: write
      pull-requests: write  # required for sticky PR comment (doc preview link)
    uses: ./.github/workflows/build-docs.yml
    with:
      host-platform: linux-64

  # TODO: Coverage runs take over 2 hours; need to figure out why it's running so slow.
  # coverage-report:
  #   needs:
  #     - build-linux-64
  #   permissions:
  #     contents: read  # This is required for actions/checkout
  #   uses: ./.github/workflows/test-wheel-linux.yml
  #   with:
  #     build-type: pull-request
  #     host-platform: linux-64
  #     test-script: run-coverage-tests
  #     test-dependencies: "jq wget libgl1 libegl1 sqlite3"
  #     test-dependent-exes: "jq wget sqlite3"
  #     matrix_filter: 'map(select(.ARCH == "amd64" and .CUDA_VER == "12.9.1" and .PY_VER == "3.12")) | .[0:1]'

  # =============================================================

  checks:
    name: Check job status
    if: always()
    runs-on: ubuntu-latest
    needs:
      - should-skip
      - test-linux-64
      - test-linux-aarch64
      - test-windows
      - test-expensive-linux-64
      - test-expensive-linux-aarch64
      - test-cudf-source-linux-64
      - test-cudf-source-linux-aarch64
      - test-numbast-mlir-linux-64
      - test-numbast-mlir-linux-aarch64
      - test-nvmath-python-mlir-linux-64
      - test-nvmath-python-mlir-linux-aarch64
      - test-cuda-gdb-linux-64
      - test-cuda-gdb-linux-aarch64
      - build-docs
      # - coverage-report
      # - test-simulator
    steps:
      - name: Exit
        run: |
          # GitHub treats `result == 'skipped'` as success for required
          # status checks (see CCCL gate comment + cccl#605). The previous
          # `cancelled || failure` predicate let upstream build failures
          # propagate as `skipped` on downstream test jobs and silently
          # pass this aggregator. Adopt CCCL's `check_result` pattern:
          # require an explicit `expected` status per dependency, where
          # anything else (including `skipped` from a failed upstream)
          # fails the gate. `if: always()` on the job still ensures this
          # step runs even when needs are skipped.
          if [[ "${{ needs.should-skip.outputs.skip }}" == "true" ]]; then
            echo "[no-ci] - skipping aggregator checks"
            exit 0
          fi

          doc_only="${{ needs.should-skip.outputs.doc-only }}"
          status="success"
          check_result() {
            name=$1; expected=$2; result=$3
            echo "Checking $name: result='$result' (expected '$expected')"
            if [[ "$result" != "$expected" ]]; then
              echo "::error::$name did not match expected result"
              status="failed"
            fi
          }

          # always expected to succeed (even in [doc-only] mode)
          check_result "should-skip" "success" "${{ needs.should-skip.result }}"
          check_result "build-docs"  "success" "${{ needs.build-docs.result }}"

          # [doc-only] flips these from 'success' to 'skipped'
          if [[ "$doc_only" == "true" ]]; then expected="skipped"; else expected="success"; fi
          check_result "test-linux-64"                   "$expected" "${{ needs.test-linux-64.result }}"
          check_result "test-linux-aarch64"              "$expected" "${{ needs.test-linux-aarch64.result }}"
          check_result "test-windows"                    "$expected" "${{ needs.test-windows.result }}"
          check_result "test-expensive-linux-64"         "$expected" "${{ needs.test-expensive-linux-64.result }}"
          check_result "test-expensive-linux-aarch64"    "$expected" "${{ needs.test-expensive-linux-aarch64.result }}"
          check_result "test-cudf-source-linux-64"       "$expected" "${{ needs.test-cudf-source-linux-64.result }}"
          check_result "test-cudf-source-linux-aarch64"  "$expected" "${{ needs.test-cudf-source-linux-aarch64.result }}"
          check_result "test-numbast-mlir-linux-64"      "$expected" "${{ needs.test-numbast-mlir-linux-64.result }}"
          check_result "test-numbast-mlir-linux-aarch64" "$expected" "${{ needs.test-numbast-mlir-linux-aarch64.result }}"
          check_result "test-nvmath-python-mlir-linux-64"      "$expected" "${{ needs.test-nvmath-python-mlir-linux-64.result }}"
          check_result "test-nvmath-python-mlir-linux-aarch64" "$expected" "${{ needs.test-nvmath-python-mlir-linux-aarch64.result }}"
          check_result "test-cuda-gdb-linux-64"          "$expected" "${{ needs.test-cuda-gdb-linux-64.result }}"
          check_result "test-cuda-gdb-linux-aarch64"     "$expected" "${{ needs.test-cuda-gdb-linux-aarch64.result }}"

          [[ "$status" == "success" ]]
